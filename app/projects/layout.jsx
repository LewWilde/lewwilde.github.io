const ProjectsLayout = ({ children }) => {

    return children

}

export default ProjectsLayout;

export const metadata = {
    title: {
        default: 'Project', // a default is required when creating a template
        template: '%s | Projects',
    },
}